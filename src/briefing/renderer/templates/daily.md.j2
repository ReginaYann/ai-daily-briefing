# AI Daily Briefing — {{ date }}

> 共 {{ total }} 条 · 分组方式：{{ group_by }} · 生成时间 {{ generated_at }}
{% for group_name, items in groups -%}

## {{ group_name }} ({{ items|length }})
{% for it in items %}
### {{ it.title }}
- **TL;DR**：{{ it.tldr }}
- **为什么重要**：{{ it.why_matters }}
{%- if it.work_relevance %}
- **与我研究的关联**：{{ it.work_relevance }}
{%- endif %}
{%- if it.paper_method %}
- **核心方法**：{{ it.paper_method }}
- **开源**：{{ "是" if it.is_open_source else ("否" if it.is_open_source == 0 else "未知") }} · **Benchmark 提升**：{{ "是" if it.has_benchmark_gain else ("否" if it.has_benchmark_gain == 0 else "未知") }} · **值得精读**：{{ "★ 是" if it.worth_deep_read else "否" }}
{%- endif %}
- **来源**：`{{ it.source }}`{% if it.authors_list %} · {{ it.authors_list[:5] | join(", ") }}{% if it.authors_list|length > 5 %} 等{% endif %}{% endif %}{% if it.categories_list %} · 标签：{{ it.categories_list | join(" / ") }}{% endif %}
- **链接**：<{{ it.url }}>

{% endfor -%}
{% endfor %}
---
*Generated by ai-daily-briefing.*
